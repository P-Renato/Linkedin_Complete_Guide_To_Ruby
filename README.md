# Linkedin_Complete_Guide_To_Ruby


to install on Ubuntu `sudo apt-get install ruby-full`


Run `ruby -e` to execute any command, for example `ruby -e puts 123`

Or run `irb` to execute directly in terminal